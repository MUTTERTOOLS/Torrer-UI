import { withInstall } from '@torrer-ui/utils';
import row from './src/row/row.vue';
import col from './src/col/col.vue';

export const TrRow = withInstall(row);
export const TrCol = withInstall(col);
