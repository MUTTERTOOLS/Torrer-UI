import { withInstall } from '@torrer-ui/utils';
import breadcrumb from './src/breadcrumb/breadcrumb.vue';
import breadcrumb_item from './src/breadcrumb-item/breadcrumb-item.vue';

export const Breadcrumb = withInstall(breadcrumb);
export const BreadcrumbItem = withInstall(breadcrumb_item);
